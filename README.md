# EndBrain

EndBrain is a local, single-process knowledge graph package for Python.

It provides:
- In-memory graph operations and keyword query
- Delayed persistence with flush/compact APIs
- Lifecycle scoring and eviction with memory pressure awareness
- JSON-serializable metadata on entities and relations
